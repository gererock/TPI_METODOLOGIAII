# Base de datos (PostgreSQL)
DB_URL=jdbc:postgresql://localhost:5432/bodypaint
DB_USERNAME=bodypaint_user
DB_PASSWORD=changeme
DDL_AUTO=update

# Credenciales de administrador (login fijo por variable de entorno, no persiste en base de datos)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=changeme

# Credenciales de vendedor (login fijo por variable de entorno, no persiste en base de datos)
VENDEDOR_EMAIL=vendedor@example.com
VENDEDOR_PASSWORD=changeme
